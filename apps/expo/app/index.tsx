import { HomeScreen } from 'app/features/home/screen'
import { ScrollView } from 'react-native-gesture-handler'

export default function Home() {
  return <ScrollView className='bg-zinc-900'><HomeScreen /></ScrollView>
}
