# Session log — issues & design decisions

Session `dd5cc11a-272f-4464-9f3b-3f1d710f429f` · working dir `gaudiyakirtan-mono` ·
2026-07-22 16:20. **Transcript-derived** (rate-limited before self-authoring). A short **local-dev
ops** session, not a feature/design session.

## What happened
- **16:20 — "open up and run web locally."** Started the web dev server (`pnpm dev` in `web/`) and
  opened it; a mobile-viewport screenshot was reviewed.
- **16:29 — "close all of them now."** Stopped the running dev server(s).

## Notable point (recurring across sessions)
- The only durable lesson here is the one that recurs in the other logs: **leftover `next dev` /
  `next start` servers hold `.next/` and collide with a later `next build`** (surfacing as
  `ENOENT` on `.next/export/*`). Cleanly stopping dev servers before building avoids the false
  "build failure" that several sessions had to diagnose.

## Design decisions
None. No spec or code decision originates here.
